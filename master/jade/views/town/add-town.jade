.panel.container-fluid(ng-controller="AddTownCtr as vm")
    .panel-heading.text-bold 新增商圈
    .panel-body.row
        form.form-horizontal(style="padding: 130px 130px 0 130px;")
            .form-group.col-md-6
                label.col-md-3.control-label.text-center(for="code") 商圈代码：
                .col-md-8
                    input.form-control(id="code" type="text" placeholder="请输入商圈代码" ng-model="vm.town.code")
            .form-group.col-md-6
                label.col-md-3.control-label.text-center(for="name") 商圈名称：
                .col-md-8
                    input.form-control(id="name" type="text" placeholder="请输入商圈名称" ng-model="vm.town.name")
            .form-group.col-md-6
                label.col-md-3.control-label.text-center(for="topSearchForm") 所属于区域代码：
                div.limit-width.col-md-8(id="topSearchForm")
                    div.item-content
                        ui-select(ng-model="vm.regiongAsync.selected" theme="bootstrap" on-select="vm.onSelectCallback($item)"  style="min-width: 300px;" title="请选择停车场")
                            ui-select-match(placeholder="请选择区域") {{$select.selected.name || $select.selected}}
                            ui-select-choices(repeat="region.name as region in vm.regiongAsync | propsFilter: {name: $select.search}")
                                div(ng-bind-html="region.name")
                                small
                                    | 区域名称：
                                    span(ng-bind-html="'' + region.name")
    .panel-footer
        button.btn.btn-primary.pull-right(type="button" ng-click= "vm.addTown()") 确认添加
        button.btn.btn-danger(type="button" ng-click= "vm.backToPrevious()") 返回上一级菜单
